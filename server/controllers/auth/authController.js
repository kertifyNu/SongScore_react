const callback = require("./callback");
const login = require("./login");
const returnAccessToken = require("./returnAccessToken");
//const player = require("./player");

module.exports = { callback, login, returnAccessToken };
